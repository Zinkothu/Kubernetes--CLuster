
kubectl port-forward service/dashboard 9002:9002 --address 192.168.100.2


kubectl exec -it <pod-name> -- sh


apk update
apk add bind-tools


echo "# Kubernetes--CLuster" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin git@github.com:Zinkothu/Kubernetes--CLuster.git
git push -u origin main

#ssh
ssh-keygen -t ed25519 -C "your_email@example.com"
cat ~/.ssh/id_ed25519.pub
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519

ssh -T git@github.com



![Alt text](./Kubernetes%20DNS.png)
